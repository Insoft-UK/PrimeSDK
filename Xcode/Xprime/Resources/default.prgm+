#pragma mode( separator(.,;) integer(h64) assignment(:=) indentation(2) operators(▶,*,/,-,+) )

SETUP: app::setup();
LOOP: app::loop();
RESET: app::reset();

START: app::start()
begin
    app::setup();
    app::loop();
    app::reset();
end;

app::setup()
begin
end;

app::loop()
begin
end;

app::reset()
begin

end;
